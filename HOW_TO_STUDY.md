# 持续学习流程

默认学习顺序由 [ROADMAP.md](ROADMAP.md) 统一定义。新读者先做 M00 诊断，复制 [个人进度模板](templates/learner-progress.md)；[PROGRESS.md](PROGRESS.md) 记录仓库维护者的实际工作，不是读者的课程成绩。原始项目编号只作索引。

## 每次学习的最小循环

1. 读当前课程模块与自己的进度；维护者另读 [PROGRESS.md](PROGRESS.md)。只选一个主问题，例如“被 mask 的目标为何不进入全局 token 分母”。
2. 确认该仓库 SHA 与本次笔记一致；若源码已更新，保留旧证据并建立新的记录。
3. 阅读调用方、实现、配置与一个相关测试。记录输入/输出 shape、对象拥有者、发生副作用的位置。
4. 把事实、作者宣称、推断与未验证假设分开。引用固定 commit 和行号，避免仅链接不断变化的 main。
5. 先写一个最小的可否证预测，再决定是否需要实验。记录控制变量、指标、实际命令与结果；无需 GPU 的部分先在本地完成。
6. 更新项目笔记和知识树中的一个节点；补一条有证据的连接。只有真正跟踪过的链路才升级学习等级。
7. 在 session 记录写下本次产物与下一项明确动作，然后更新进度。

每个模块分别积累两类证据：知识理解以机制解释、推导或设计图为证；工程能力以可检查的实现、配置、运行记录或故障分析为证。未运行的设计标明“待验证”，并记录本次从需要帮助到可以独立完成的具体变化。

## 笔记标准

源码笔记采用 [项目笔记模板](templates/repository-note.md)，研究选择用 [决策模板](templates/research-decision.md)。一份有效笔记应能回答：

- 这层系统消费什么、产生什么？
- 谁拥有状态？谁能修改它？哪些内容被持久化？
- 正常路径、取消/超时/失败路径有什么不同？
- 哪一个量需要保持不变，哪个参数是在交换成本？
- 怎样用实验发现自己的解释错了？

“这个项目支持 GRPO / MoE / async”只能作为入口，不能替代上述解释。

## 关系类型

| 类型 | 判定证据 | 不应据此推导 |
|---|---|---|
| 直接依赖 | import、manifest、submodule/gitlink | 任意两个 HEAD 都兼容 |
| 可选后端 | guarded import、backend 分支、optional extra | 默认路径一定使用该后端 |
| 示例集成 | example 实际调用；或明确标注仅是 pointer | 整个项目默认端到端接好 |
| 项目谱系 | 官方 fork/派生说明 | 当前 API 完全一致 |
| 概念对应 | 相同问题的独立实现，并列源码证据 | 两个软件互相依赖 |

## 实验规则

实验放 `experiments/NNN-topic/`，至少有 `README.md`、运行脚本、小型结果。记录模型/数据/源码版本、环境和随机性来源。模型权重、数据集与大 checkpoint 不进学习仓库 Git。

本次只做本地无付费 API 的 CPU 实验。后续需要容器、GPU、云服务或模型 API 的实验，应先把具体命令、资源需求和预期产物写清楚，再在具备相应环境时运行。引用 recipe 不代表训练数据和依赖已备齐。

## 源码管理

- 主仓库是 [yubol-bobo/frontier_llm_handbook](https://github.com/yubol-bobo/frontier_llm_handbook)，默认同步目标为 `origin/main`；后续学习资料继续在此积累。
- `sources/` 由每个 upstream 自己的 Git 管理；外层 Git 忽略该目录。
- 默认克隆浅历史、不初始化子模块、不下载 LFS 内容。按需研究某一旧版本时，显式获取该版本，不重置其他已有改动。
- `sources.lock.json` 是学习来源记录，不是 21 个项目联合可运行的依赖锁。
- 阅读记录使用 GitHub commit permalink；本地链接用于快捷跳转。
- 独立源码 HEAD 与某个项目锁定的依赖 commit 可能不同。实际运行以该项目的依赖锁定为准。

## 续学提示词

可以在这个学习仓库里继续说：

> 读取 PROGRESS.md，继续当前学习单元。先核对源码 SHA，沿一个具体实现问题读代码，记录证据；选择适合当前环境的小实验；更新项目笔记、知识树、关系和进度。不要把未执行的训练写成已复现。

这份仓库保存学习状态；它本身没有配置定时运行、后台训练或自动更新 upstream。
