# 怎样把一份公开材料变成可学习的知识

本项目服务于没有 frontier lab 内部资源的学习者。贡献应让一个概念更容易理解、检查或实践。定位与顺序见 [ROADMAP](ROADMAP.md)。

## 新资源的最低信息

请说明对应模块、先修、核心问题、优先阅读的章节/文件、预期产物，以及它补了哪一个 [覆盖缺口](COVERAGE.md)。给出作者或项目官方来源、访问日期、版本，并区分论文、权重、训练代码、推理代码、数据和运行记录的公开程度。只有名字和网址的推荐先保留为候选，不直接进入核心阅读。

## 新 lesson 的结构

先给一个具体问题和输入输出，再解释机制；提供 worked example、至少一个错误例子、源码入口、读者练习和验收答案或判据。写清 CPU 可完成的部分与需要 GPU 的部分。面向中文读者解释术语，并保留英文名称方便继续读源码。

## 实验与研究结论

使用 [实验/决策模板](templates/research-decision.md)，记录版本、数据、环境、命令、指标、原始结果和限制。不要用模拟器速度报告 GPU 性能，不用训练集 reward 替代独立能力；失败、丢弃和预算变化必须说明。大数据、权重和凭据不放入学习仓库。

## 修改与验证

保留历史证据，新版本建立新的记录，不把动态 main 链接覆盖成旧运行的事实。修改课程后同步导航、模块关系、资源地图与覆盖状态，避免同时存在两套默认学习顺序。

在已恢复源码的本地仓库运行 `python tools/validate_learning_repo.py` 检查文档与固定引用。涉及可运行例子的修改还要运行相应例子，保存与声明匹配的实际结果。验证通过后按既有 Git 工作流提交；本仓库没有自动定时抓取或后台训练。
