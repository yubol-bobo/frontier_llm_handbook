# 2026-09-08 RL 源码阅读审计

阶段：L1；范围：6 个 RL/环境/recipe 仓库。任务是在本地形成可继续追踪的学习材料，不是运行训练。

## 本次动作

在学习库根目录执行 `python tools/clone_repos.py --group rl --workers 2`，6 个仓库均返回 `status=cloned`。工具采用 shallow clone，不初始化 submodules，并跳过 Git LFS 内容下载。未运行上游安装脚本、模型/API 请求、沙箱启动、GPU 作业或训练测试；未发布到任何外部服务。

| 仓库 | 完整 snapshot SHA | 实际阅读的主要实现 |
|---|---|---|
| verl | `7cb65014d3a6c84f59458367df768999e4f36c67` | `ray_trainer.py` 的 fit/advantage；`core_algos.py` 的 GRPO；Megatron engine imports；setup extras |
| slime | `4c193f1f37509cca70f0e88807a9305b70f63f4e` | `train.py`；`agent/trajectory.py`；SGLang process entry；Megatron actor imports；branching tests 的组织 |
| Miles | `3de96596f16b9e6d23ba550c4c47de3479c9f14c` | `replay_base.py`；`replay_data.py`；Megatron actor replay stages；MoE layer mapping |
| Prime RL | `04a61d3b75c3c99f263b2c133e822f998909adf7` | `train_source.py`；`train_sink.py`；`orchestrator.py`；`algo/grpo.py`；pyproject/submodule |
| Verifiers | `27bbd216df0af719a43705866b2cf6139bcc95de` | v1 `rollout.py`；`task.py`；Pi harness adapter；Harbor taskset adapter |
| APEX recipe | `8e7702f03b7464a36ab800a624fd911de0968a87` | `agents/tito.py`；`tito_harbor_generator.py`；fully async entrypoint；pyproject |

已阅读 verl、Miles、Prime RL、Verifiers 根目录 AGENTS.md；它们有关 upstream 修改、测试和 PR 的规则未被误用为本地笔记的确认门槛。本次没有修改上游源码或提交上游贡献。

## 新增材料

- [verl](../repositories/verl.md)
- [slime](../repositories/slime.md)
- [Miles](../repositories/miles.md)
- [Prime RL](../repositories/prime-rl.md)
- [Verifiers](../repositories/verifiers.md)
- [APEX recipe](../repositories/apex-agents-skyrl-recipe.md)
- [关系与版本边界](../connections/rl.md)

六篇按模板覆盖：问题、至少两个实际实现文件、执行/数据流、机制与取舍、真实依赖和概念对应的区分、待执行实验、下一条代码路径。每篇有 4 个固定 commit 且带行号的主要源码链接。链接行号依据本地文件长度与所读位置校验。

## 影响后续学习的重要发现

1. slime 当前实现给每个导出的 Sample 全额 outcome reward，并对共享生成前缀训练去重；不能沿用旧网页的 reward/K 说法。
2. 当前 Verifiers 要按 v1 Task/Harness/Runtime/Rollout 阅读；Prime RL 也实际导入 `verifiers.v1`。
3. Prime RL 与 verl 的所读 GRPO credit/advantage 函数默认归一化不同，算法名不足以证明实现等价。
4. Prime RL 依赖的 Verifiers gitlink 与独立 clone HEAD 不同；APEX 的 SkyRL 路径只在作者机器布局下成立。19 个源码目录不代表 19 套运行环境已经配置。

## 验证结论与未覆盖部分

已完成 clone、源码静态阅读、笔记写入、固定链接行号/本地路径校验。未执行文中的实验，因而无收益、速度、loss 或 benchmark 数字可报告。依赖版本兼容、sandbox 权限、模型与数据准备、完整异步 trainer 执行、GPU 数值稳定性均留待相应 L2/L3 工作。
