# 2026-09-08 Training 组源码初读审计

范围：6 个源码 snapshot、6 份 L1 笔记、1 份跨库连接笔记。记录执行过的工作与未执行的验证，避免把开源能力说明误记成本地实验结果。

## 源码快照

执行 `python tools/clone_repos.py --group training --workers 2`，六库 clone 成功。为 `--depth 1 --no-tags` shallow snapshots；没有拉取历史、submodules 或 LFS payload，没有安装上游依赖。

| 仓库 | HEAD | tracked files | 特殊项 |
|---|---|---:|---|
| smollm | `a041759883ec7152d18fb985ea49be641a0bceef` | 711 | 无检测到的 LFS pointer / submodule |
| olmo-core | `92870a33c3fee060d57c3faec52b0b369ece85a6` | 531 | 无检测到的 LFS pointer / submodule |
| open-instruct | `ebd0c8e0c1d778085a4f26aacc2fd4d96f3515f5` | 751 | 4 个 LFS pointers，payload 未拉取 |
| marin | `5e2436d0f61462983003bd8b6eaef8235ecab78c` | 3831 | 无检测到的 LFS pointer / submodule |
| torchtitan | `4e20e76b235ec415bd81912ce09eed46fa39f717` | 739 | 无检测到的 LFS pointer / submodule |
| megatron-lm | `c9b53d0a87cb926f47115259593ecfeb351ca29f` | 3216 | 无检测到的 LFS pointer / submodule |

Open Instruct 未拉取的是 `open_instruct/test_data/{preference_sample.jsonl,rlvr_sample.jsonl,sft_sample.jsonl,tokenizer/tokenizer.json.gz}`。因此连部分上游测试 fixtures 都还未备齐，不能宣称该库可以立即通过原始测试。clone 后元数据记录在 `.cache/clone/<id>.json`；统一 lock 的生成由主任务处理。

已读相关 AGENTS：OLMo-core、Open Instruct、Marin 根及 lib/marin / experiments、TorchTitan 根指向的 `.claude/CLAUDE.md`、Megatron 根。未修改任何上游源文件；不涉及其 PR、安装、集群运行工作流。

## 交付与阅读范围

- [SmolLM](../repositories/smollm.md)：4K / 32K YAML、FineMath tokenization、LoRA SFT 脚本；算出两个配方均为 2,359,296 tokens/update。
- [OLMo-core](../repositories/olmo-core.md)：正式 Olmo 3 配方、Trainer._fit_epoch、TransformerTrainModule 的 microbatch、loss 与 optimizer 逻辑。
- [Open Instruct](../repositories/open-instruct.md)：GRPO step、rho correction / DAPO / CISPO、权重同步、OLMo-core SFT 导入及 dependency pin。
- [Marin](../repositories/marin.md)：ArtifactStep/StepContext、lower/run、fingerprint、train_lm builder、mixture 与单 step runner。
- [TorchTitan](../repositories/torchtitan.md)：当前声明式 sharding、parallelize 组合、训练入口、有效 token 汇总与 optimizer finite check。
- [Megatron-LM](../repositories/megatron-lm.md)：MoE forward / TopKRouter、Flex / DeepEP manager、fused_a2a Buffer bridge。
- [连接笔记](../connections/training.md)：明确区分直接依赖、可选后端、示例依赖与概念对应。

## 观察到的边界

Open Instruct 所 pin 的 OLMo-core commit 不同于独立 clone HEAD；SmolLM 的训练配置带内部集群路径，FineMath 示例读未在本文件 parser 定义的 args.email；TorchTitan 当前入口/核心文件布局已不同于较老教程；Marin 的 fingerprint 是配置指纹，不是完整源码内容 hash；Megatron 的 DeepEP 是可选后端，不是默认全部 MoE 路径。

上述行为是静态源码发现或明确标注的推导，没有冒充运行失败、性能改善或模型效果。

交付校验：8 份笔记中的 32 个 commit 固定源码链接逐个检查本地文件、HEAD SHA 和行号范围，错误 0；50 个本地 Markdown 链接目标全部存在。六个 upstream working tree 的 tracked changes 均为空。每份 repository note 有 4 个固定源码证据链接。

## 没有执行

没有训练、GPU 测试、收敛验证、模型/data 下载、Beaker/Slurm/云作业提交、安装脚本、镜像构建、外部发布。笔记内的实验均标“待执行”，未生成伪造结果。文件链接、固定 SHA 和源码行号做本地静态校验；这类校验不提升学习阶段到 L3。
