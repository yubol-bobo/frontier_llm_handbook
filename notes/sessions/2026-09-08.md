# 2026-09-08：建立学习仓库，完成首轮源码定位与奖励实验

## 本次目标与实际产物

把最初四个项目、推荐清单和 Megatron-LM 汇总成一个本地学习仓库。19 个仓库浅克隆完成，固定来源与 SHA；外层 Git 管理学习资料，源码保持独立。初读分成 harness、训练、RL、infra 四组，逐仓库记录所读实现与证据，最终汇入一棵概念树与一份有类型的依赖图。

实际产物：[学习清单](../../LEARNING_LIST.md)、[源码索引](../../SOURCE_INDEX.md)、19 篇项目笔记、4 篇连接专题、[知识树](../../KNOWLEDGE_TREE.md)、[进度](../../PROGRESS.md)、[实验 001](../../experiments/001-harbor-reward-contract/README.md)。

## 主线阅读

1. Pi：读 agent-loop.ts 的上下文转换、嵌套循环、steering、工具 preflight、并发完成与结果顺序，并对照 types.ts 的回调约定。只对这条路径记 L2。
2. DeepSeek Harness：读 preStep/turn/step/buildRequest 与 invariant companion，理解“模型可见内容由日志重建”的具体实现与适用范围。
3. Harbor Cookbook：读 multi-reward、多步骤任务、harbor_rl adapter；发现部分 RL 目录是 pointer，且某一训练例依赖 feature branch。

## 一个已运行实验

使用固定 Git 对象里的参考解及 Python 断言，比较参考解与去掉邮箱大小写归一化的错误版本。参考解 correctness=1/performance=1；错误版 correctness=0/performance=1。保存逐断言结果、耗时与输入哈希。未启动 Docker、Harbor runtime 或训练器。

审阅后补强实验：从固定 SHA 读取执行文件，防止工作区变化污染归因；拒绝 Python optimized mode，防止 assert 被移除后出现虚假通过。

## 本次建立的连接

- Verifiers → Pi：真实 ACP/provider harness adapter；Verifiers → Harbor：真实 taskset adapter。
- Prime RL → TorchTitan：Linux dependency 与组件复用，不能误认为整个 trainer 相同。
- Open Instruct → OLMo-core：直接依赖且 pin；独立 clone HEAD 不同。
- slime/Miles/verl → Megatron/SGLang：训练与生成后端连接，角色与版本要分别标明。
- Megatron/SGLang → DeepEP、SGLang → DeepGEMM：可选后端，接口与硬件条件不可省略。

## 本次没有完成的事

没有完整阅读所有代码、复现大模型训练、配置 GPU 集群、安装 19 套运行环境、下载权重/训练数据、初始化全部第三方子模块或创建远程仓库。未把 README 功能宣称、静态推断和实际测量混为一谈。

## 下一项动作

进入 Pi 的 durable runtime / reducer / session 路径，解释一次取消与恢复；以代码证据修订当前简化循环，再准备一个 keyless 事件 trace。见 [PROGRESS.md](../../PROGRESS.md)。
