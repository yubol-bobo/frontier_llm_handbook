# 2026-09-08 Infra 源码初读审计

本轮执行 `python tools/clone_repos.py --group infra --workers 2`，四个项目均克隆成功。没有安装依赖、执行 upstream 安装脚本、初始化子模块或运行 Docker/GPU benchmark。读取 Harbor 上游 AGENTS；笔记写在独立学习库中，未修改源码 checkout。

| 项目 | 实际 HEAD | 本轮实现阅读重点 |
|---|---|---|
| Harbor | `9a2e3b135cc8fb1e41131020f83370cb2f12ae93` | 单步 trial、共享/独立验证环境、reward 解析与结果字段 |
| SGLang | `30e7a3072d3f1e9bd70cd5e44146ca27c80522c4` | 两种 scheduler loop、prefill 合并、radix 引用/驱逐、DeepEP v2 adapter、DeepGEMM wrapper |
| DeepGEMM | `559d79fb6994a58b8a15b4b93bf13ccc16edf247` | GEMM 参数契约、SM100 配置与代码生成、JIT cache 发布、Mega MoE 分离对照 |
| DeepEP | `01dc3aaac82068020353dce2c302e38153c0bfaa` | balanced gate 的 SM 模型、cached handle、combine、CUDA event 与 NCCL 生命周期 |

产出：四篇 `notes/repositories/` L1 笔记与 [infra 连接审计](../connections/infra.md)，每篇含四个固定 commit 的源码链接、函数/数据流、事实与推断区分、尚未执行的实验。

主要修正：DeepEP 当前源码有 V2 接口；SGLang 存在实际 V2 adapter；DeepGEMM 的 DeepEP 关系是可选对照基准；Cookbook 的 harbor.rl 示例单独指向 feature 分支，因此不能混用 main 合约作端到端断言。Harbor 默认 verifier 保留多维字典，未自动标量化。

验证方式：本地源码逐段阅读、路径/符号检索、克隆返回 SHA 检查、Markdown 固定链接的文件与行号范围检查。没有新增运行性能或训练结果；根任务另行开展的 CPU 教学实验不计入本 infra 阅读者的实测。

下一轮：选定一个可执行的最小实验进入 L2/L3，而非继续增加仓库数量。优先用单卡做 SGLang 前缀缓存对照；若暂时没有 GPU，则先用 Harbor 小任务验证 reward/产物协议。
